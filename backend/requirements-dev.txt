-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
aiosqlite==0.20.0
# SQLAlchemy's async engine needs greenlet at runtime; usually pulled in
# as a transitive dep, but Python 3.13 wheels can be missing on some platforms.
greenlet>=3.1.0
